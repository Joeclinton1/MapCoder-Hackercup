C++: |
  Include ALL standard libraries needed.
  Declare ALL variables
  
  ## Note: Strictly follow the input and output format.
  The input should be taken from standard input (std::cin), and the output should be given to standard output (std::cout).
  If you are writing a function, after the function definition, take input using std::cin, then call the function with the specified parameters, and finally provide the output of the function using std::cout.
  Do not add extra output statement otherwise it will failed the test cases.
  
  Begin with
  #include <climits>
  #include <iostream>
  #include <iomanip>
  #include <numeric>

Python3: |
  Handle limits with `float('inf')` or `sys.maxsize`.
  use built in libraries only
  Use sys.setrecursionlimit if problem requires higher precision
  
  ## Note: Strictly follow the input and output format.
  The input should be taken from Standard input and output should be given to standard output. 
  If you are writing a function then after the function definition take input using input() function then call the function with specified parameters and finally print the output of the function. 
  Do not add extra print statement otherwise it will failed the test cases.